<MudProgressLinear Color="Color.Primary" Value="@Value" Size="Size.Large" Rounded="true" Class="my-7">
    <MudText Typo="Typo.caption" Color="Color.Warning">
        <b>@(Math.Round(Value, 2))%</b>
    </MudText>
</MudProgressLinear>

@code {
    [Parameter] public double Value { get; set; }
}