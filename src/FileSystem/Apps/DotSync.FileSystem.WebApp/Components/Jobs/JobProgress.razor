<div class="overflow-auto progress-window log-font">
    @foreach (var msg in Messages ?? [])
    {
        @msg<br/>
    }
</div>

@code {
    [Parameter] public IReadOnlyList<string>? Messages { get; set; }
}